/** User-facing text when Firebase rejects the operation because the provider is off in the project. */
export const FIREBASE_OPERATION_NOT_ALLOWED =
    'This sign-in method is disabled in Firebase. Open Firebase Console → Build → Authentication → Sign-in method, then enable Email/Password and/or Google.';
